// The app and the component library hash label names to the same eight hues, so a pill in
// a records table and one on a task card never disagree; the implementation lives in ui.
export { colorForLabel } from '@/ui/ai/list-format';
